# Phase 6：现场验收记录

## 当前状态

检查日期：2026-07-12

| 项目 | 状态 | 结果 |
| --- | --- | --- |
| PyInstaller x64 one-folder 构建 | 通过 | `BMS-CAN-Monitor.exe` 已生成 |
| 发行包关键资源 | 通过 | DBC 与 x64 `ControlCAN.dll` 均位于 `_internal` 预期路径 |
| EXE 与 DLL PE 架构 | 通过 | 均为 AMD64 |
| 打包 GUI/DBC 演示冒烟测试 | 通过 | 2026-07-12 清除 Python 环境变量并从临时目录启动后自动正常退出 |
| 发行目录体积 | 通过 | 约 159.7 MiB，zip 约 64.6 MiB，未包含 matplotlib/Pillow |
| 单台 CANalyst-II 数据接收 | 通过 | 用户已确认接入设备后可显示实时数据 |
| 单台 SQLite 记录 | 通过 | 用户已确认实机数据可以正常记录 |
| 多 BMS 自动化闭环 | 通过 | 5 个合成地址完成状态、温度、波形和 raw-only 回放隔离测试 |
| 30 分钟等效记录负载 | 通过 | 5 地址、100 万帧约等效 31.2 分钟；45,005 帧/s，70.7 bytes/帧 |
| 1024x680 / 1440x900 GUI 检查 | 通过 | 总览卡片、5 地址标签、15 条波形和图例无重叠或截断 |
| 5 台 BMS 30 分钟连续接收 | 待现场 | 需要 5 台地址唯一的真实设备和完整总线接线 |
| 5 台 BMS 2 小时波形与 SQLite 记录 | 待现场 | 需要记录实际文件增长、CPU、内存和丢帧数 |
| USB 拔出/重连 | 待现场 | 当前没有可枚举设备 |
| 非零 BMS 地址 | 待现场 | 自动化测试通过，仍需真实地址偏移报文确认 |
| Ctrl_INFO 首次真实发送 | 待台架审批 | 自动化测试只使用假适配器，未发送真实控制帧 |
| 无 Python 的干净 Windows 电脑 | 待现场 | 当前仅验证隔离环境启动，不等同于干净机 |

## 只读接收验收

设备接入后先执行只枚举：

```powershell
python -B tools\canalyst2_check.py
```

确认设备信息正确后，使用只接收方式打开 CAN1 五秒：

```powershell
python -B tools\canalyst2_check.py --channel 0 --bitrate 250000 --receive-seconds 5
```

通过条件：

- 不返回 `0xFFFFFFFF` 或适配器错误。
- 能收到周期帧，帧 ID、标准/扩展标志和 DLC 合理。
- GUI 总压、电流、SOC、单体和波形持续更新。
- 接收队列深度不会持续增长，解析失败数量稳定。

## 5 台 BMS 接线与地址表

现场开始前填写，不允许地址重复：

| BMS | 配置地址 | 物理位置/编号 | 预期电芯数 | 终端位置 | 实际识别 |
| --- | --- | --- | --- | --- | --- |
| 1 |  |  |  |  |  |
| 2 |  |  |  |  |  |
| 3 |  |  |  |  |  |
| 4 |  |  |  |  |  |
| 5 |  |  |  |  |  |

接线通过条件：

- 5 台地址均位于 `0..11` 且互不重复。
- 所有设备为 250 kbps，并连接同一个已选 CAN 通道。
- 只在总线物理两端安装 120 欧终端电阻；断电测得 CAN-H 与 CAN-L 接近 60 欧。
- 支线尽量短，CAN-H/CAN-L/GND 连续，无任何 CAN 线连接电池正负极。
- 只听模式下出现 5 个总览标签，CAN 报文页的 BMS 地址与规划表一致。

记录总线负载基线：

| 项目 | 开始值 | 结束值 | 通过条件 |
| --- | --- | --- | --- |
| 总帧率 |  |  | 稳定，无异常突降 |
| 接收队列深度 |  |  | 可回落，不持续增长 |
| 源丢帧数 |  |  | 0，或有明确原因和记录 |
| 解析失败数 |  |  | 未知帧构成可解释且不持续异常增长 |
| 记录队列深度 |  |  | 可回落，不持续增长 |

## 长稳验收

### 30 分钟接收

- 使用只听模式，不开启控制。
- 每 5 分钟记录内存、帧率、队列深度和丢帧数。
- 通过条件：GUI 操作正常，队列回落，无持续内存异常增长。

### 2 小时记录

- 使用正常模式但保持控制锁定。
- 开启 raw-only SQLite 记录，并在三个信号图中勾选 5 台 BMS。
- 结束后正常停止记录，运行 `tools\export_recording.py` 导出。
- 记录开始/结束时分别记录数据库大小、进程 CPU 和内存。
- 通过条件：数据库完整、`raw_frames` 数量合理、没有新 `signal_samples` 表、CSV 可读取、停止过程无写入错误。

### 记录回放一致性

1. 记录停止后关闭实时源，直接在 GUI 打开同一 SQLite 会话。
2. 分别以 1x 和 10x 回放，确认都出现 5 个相同地址标签。
3. 对每台 BMS 核对总压、电流、SOC、SOH、最高/最低单体和温度终值。
4. 核对每个信号图的 5 条曲线图例、地址颜色和数据身份。
5. 导出原始帧及每地址信号宽表，确认没有跨地址覆盖。

通过条件：地址集合、各地址关键终值和单体数据与实时记录一致；DBC hash 不一致时必须出现确认提示。

## 异常验收

1. 接收中拔掉 USB：程序不崩溃，显示适配器错误，记录会话可正常结束。
2. 无 CAN 报文：出现接收超时事件，UI 仍可操作。
3. 错误设备地址：保留原始帧但解析计数增加，修正地址后恢复。
4. 数据库文件占用或目录不可写：记录明确失败，不影响只读监测。
5. 审计目录不可写：控制命令在调用适配器前被拒绝。

## 控制验收门槛

真实 `Ctrl_INFO` 发送必须单独审批，并满足：台架无人员接触高压、急停可用、充放电回路状态已知、目标为默认地址 0、近期已收到目标 BMS 报文、独立审计文件可写。首次只选择一个 Mask 位并发送一帧，随后通过 `BMS_SWITCH_STATUS` 和外部测量共同确认结果。
